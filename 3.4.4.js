// создаем функцию- конструктор объектов
function SmartHome() {
    this.series = 'Comfort',
    this.power = power,
    this.color = color,
    this.timer = timer,
    this.accumulator = accumulator,
    this.wi_fi = function () {
        console.log(`connecting to wi-fi: ${wi_fi},`)
    }          
}